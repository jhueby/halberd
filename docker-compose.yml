services:
  halberd:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - halberd-data:/data
    environment:
      - HALBERD_DATA_DIR=/data
    restart: unless-stopped

volumes:
  halberd-data:
